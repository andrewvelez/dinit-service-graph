{
  "name": "dinit-service-graph",
  "type": "module",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  },
  "bin": {
    "dinit-graph": "dist/dinit-dependency-graph"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "typecheck": "tsc",
    "test": "bun test",
    "build": "bun run clean && bun run typecheck && bun test && bun build src/dinit-graph.js --compile --outfile dist/dinit-dependency-graph"
  }
}